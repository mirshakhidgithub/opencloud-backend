Django==5.2.6
djangorestframework==3.16.0
django-environ==0.12.0
django-cors-headers==4.7.0
drf-spectacular==0.28.0
psycopg[binary]==3.2.13
celery==5.4.0
redis==5.2.1
gunicorn==23.0.0
# Serves Django's own static files (admin, Swagger UI) from gunicorn, so nginx
# needs no shared volume with the container.
whitenoise[brotli]==6.12.0
cryptography==44.0.1
# TOTP for platform operators. The cabinet's second factor is Keystone's; the
# admin panel has no Keystone identity to lean on, so it computes its own.
pyotp==2.9.0
requests==2.32.3

# Tests
pytest==8.3.4
pytest-django==4.9.0
